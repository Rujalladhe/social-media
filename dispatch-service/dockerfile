FROM node:22-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install --production

# Copy application code
COPY . .

# Expose port if needed (for health checks)
EXPOSE 3000

# Start the consumer
CMD ["node", "src/rider-consumer.js"]